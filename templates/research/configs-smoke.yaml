# Smoke config: the whole pipeline in under a minute. Run before any long job.
#   just smoke      (= uv run python -m {{PACKAGE}}.run --config configs/smoke.yaml)
seed: 0
run_dir: runs/smoke
device: cpu
n_samples: 200        # tunable
n_designs: 3
